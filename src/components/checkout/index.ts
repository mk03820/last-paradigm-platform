/**
 * Checkout Components Barrel Export
 *
 * Story 17.3: Checkout Transition Experience
 *
 * Components for the checkout transition experience, providing
 * a branded loading overlay during Stripe redirect.
 */

export { CheckoutTransition, default as CheckoutTransitionDefault } from './CheckoutTransition';
export type { CheckoutTransitionProps, CheckoutTransitionStatus } from './CheckoutTransition';
